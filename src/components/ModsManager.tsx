import React from 'react';
import { ModManager } from './ModManager';

/**
 * @deprecated Use ModManager instead. Re-exported for backward compatibility.
 */
export const ModsManager = ModManager;
export default ModManager;
